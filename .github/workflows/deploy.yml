name: Deploy to Railway

on:
  push:
    branches:
      - master  # Ganti ke 'main' jika branch kamu bernama main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install Railway CLI
        run: npm install -g @railway/cli

      - name: Deploy to Railway
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_API_KEY }}
        run: railway up --project=2bc0aa0e-0e39-441d-8f71-6b5e18fa94c9 --service=ai-assistant-bot --detach

